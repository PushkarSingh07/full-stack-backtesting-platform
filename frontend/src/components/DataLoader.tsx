import { Box, Typography } from '@mui/material';

export default function DataLoader() {
    return (
        <Box>
            <Typography variant="h4">Data Loader</Typography>
        </Box>
    );
}